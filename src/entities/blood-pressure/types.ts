// Re-export types from shared layer for convenience
export type {
  BPRecord,
  BPRecordInput,
  BPRecordRow,
} from '../../shared/api/bp-repository';

export type {
  BPCategory,
  MeasurementLocation,
  MeasurementPosture,
} from '../../shared/config';
